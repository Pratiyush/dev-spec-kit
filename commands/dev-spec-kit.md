---
description: Run the dev-spec-kit spec-driven loop on a request — route, spec (EARS), evidence-bound tasks, TDD, proofs, graph, approval, PR.
---

Use the dev-spec-kit-workflow skill on the following request. Start with `dev-spec-kit route` and report the chosen
mode + reason before doing anything else. Honor every hard rule in the skill — especially: no done
without green proofs, quick mode still writes a test, and stop at the human gates.

Request: $ARGUMENTS
